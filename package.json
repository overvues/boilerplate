{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "format": "lerna run format",
    "test": "lerna run test",
    "now-build": "npm run build"
  },
  "devDependencies": {
    "lerna": "^3.13.1",
    "@types/jest": "^24.0.9",
    "@types/node": "^11.10.4",
    "jest": "^24.1.0",
    "ts-jest": "^24.0.0",
    "typescript": "^3.3.3333"
  },
  "workspaces": [
    "packages/backend",
    "packages/dynamic",
    "packages/shared-components",
    "packages/static"
  ]
}
